apiVersion: v1
kind: Service
metadata:
  name: devops-metrics-api
  namespace: devops-metrics-staging
spec:
  type: LoadBalancer
  selector:
    app: devops-metrics-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000